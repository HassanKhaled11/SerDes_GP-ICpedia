function up_dn()
fileID=fopen('Up_Dn.hex','r');
% formatSpec='%f,%';
% data=csvread(fileID,formatSpec);
data=csvread(fileID,formatSpec);
cumData= zeroes(length,2);
fclose(fileID);
len =length(data);
cumData= zeroes(length,2);

cumData[i=]=data[i][0]-data [i][1]+cumData[i-1]
for i = 2:len
cumData[i]=data[i][0]-data [i][1]+cumData[i-1]
end